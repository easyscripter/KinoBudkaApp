import { StyleSheet } from 'react-native';

const filmScreenStyles = new StyleSheet.create({
    cardList: {
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'wrap',
        justifyContent: 'space-around',
        backgroundColor: '#0c001c',
        width: '100%',
        paddingTop: 10,
    }
});

export default filmScreenStyles