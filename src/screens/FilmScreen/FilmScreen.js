import React from 'react';
import {
    Text,
    View,
    SafeAreaView,
    ScrollView
} from 'react-native';

import filmScreenStyles from './FilmScreen.styles';



const FilmScreen = ({ navigation, route }) => {
    return (
        <SafeAreaView style={{ flex: 1 }}>

        </SafeAreaView>
    )
}

export default FilmScreen;